FAB="${TOP}/bin/fab"
KEYDIR="${TOP}/etc/keys"
VAR="${TOP}/var"

for i in "${KEYDIR}"/*.keystore ; do
  NAME="$(basename "$i" .keystore)"

  if [[ "${NAME}" == "trust" ]] ; then
    continue
  fi

  mkdir -p "${VAR}"
  "${FAB}" fabric.core.GenMap "${NAME}" > "${VAR}/${NAME}"
done


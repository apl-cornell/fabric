#!/bin/bash

APP_HOME="$(dirname $0)/.."
PATH="${PATH}:${APP_HOME}/../../bin"

export FABRIC_HOME="${APP_HOME}"

fab -classpath "${APP_HOME}/classes" --name worker OO7.Benchmark fab://database/OO7 tiny 3 "$@"


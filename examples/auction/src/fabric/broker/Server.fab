package broker;

public interface Server [label Airline]  
	where {Airline} <= {*provider}
{
    int {Airline} getLowestPrice{Airline}(); 
    Agent[Airline, auction]{Airline} getAgent{Airline}(label auction) where {*auction} <= {*provider}; 
}

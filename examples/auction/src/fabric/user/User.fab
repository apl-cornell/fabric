package user;
public class User [label theUser] implements broker.User[theUser] {
    public broker.UserAgent[auction] {*auction} getAgent{*auction}(label{*auction} auction)
		where {theUser} <= {*auction}, {*provider} <= {*auction}
	{
		return new UserAgent[auction]();		
	}
}

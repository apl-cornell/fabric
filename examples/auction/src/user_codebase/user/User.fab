package user;
public class User [principal TheUser] implements broker.User[TheUser] where {*broker.User.provider} <= {*provider} {
    public broker.UserAgent[auction] {TheUser->;TheUser<-} getAgent{TheUser->;TheUser<-}(label{TheUser<-} auction)
	{
		final Store store = worker$.getLocalStore();
		return new UserAgent[auction]@store();		
	}
}

package fabnfs.util;

import fabnfs.util.ResizableFileByteArray;

public class FabFile extends FileSystemObject {

    // file attributes should be here

    ResizableFileByteArray contents;

    public FabFile fabnfs$util$FabFile(String name, FabDirectory parent, String fullPath) {
        fabnfs$util$FileSystemObject(name, parent, fullPath);
        contents = (ResizableFileByteArray)new ResizableFileByteArray().fabnfs$util$ResizableFileByteArray();
        return this;
    }
}

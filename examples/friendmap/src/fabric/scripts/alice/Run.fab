public class Run {
  public static void main(String[] args) {
    final User      alice = (User) worker$.getRemoteStore("snapp"    ).getRootMap().get("alice");
    final FriendMap app   = (User) worker$.getRemoteStore("friendmap").getRootMap().get("friendmap");

    app.viewMap(alice);
  }
}


/*
** vim: ts=2 sw=2 et cindent cino=\:0 syntax=java
*/

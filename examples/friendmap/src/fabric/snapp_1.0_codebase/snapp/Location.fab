package snapp;

// TODO: document labels

/**
 * A point on the plane.
 */
public class Location[label A]
where provider <= A
{
  public final float{this}@{A} x;
  public final float{this}@{A} y;

  public
  Location
  {      // side effects:
    this // fields are updated
  }
  (
    int{this} x, int{this} y
  )
  {
    this.x = x;
    this.y = y;
  }
}

/*
** vim: ts=2 sw=2 et cindent cino=\:0 syntax=java
*/

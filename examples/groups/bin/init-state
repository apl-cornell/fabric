#!/bin/bash

APP_HOME="$(dirname $0)/.."
FABRIC="${APP_HOME}/../fabric"
PATH="${FABRIC}/bin:${PATH}"

CP="${APP_HOME}/classes"

export FABRIC_HOME="${APP_HOME}"

fab --jvm-cp "${CP}" --debug --debug-port 8086 --app-home "${FABRIC_HOME}" --name hello \
  groups.Create

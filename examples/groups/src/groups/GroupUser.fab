package groups;

import auth.*;

public class GroupUser extends User {
	public GroupUser(String{this} name, String{this} password) where authority (this) {
		super(name, password);
	}
    
    public String{this} toJson() {
        return "{ \"name\" : \"" + name() + "\"}"; 
	}
}

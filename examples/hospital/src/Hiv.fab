public class Hiv[label hivLabel, label accessLabel] where hivLabel <= accessLabel{
  @{accessLabel}
  public boolean{hivLabel} hiv;
  public Hiv(boolean{hivLabel} hiv) 
    where {this} <= hivLabel
  {
    this.hiv = hiv;
  }
}




#!/bin/bash

APP_HOME="$(dirname $0)/.."
PATH="${PATH}:${APP_HOME}/../../bin"

export FABRIC_HOME="${APP_HOME}"
NAME=$1 ; shift 2 ;

fab --jvm-cp "${APP_HOME}/classes" --app-home "${APP_HOME}" --name "${NAME}" reedwritemix.ReadWreMixCreate01 fab://mal0/store0 "$@"



#!/bin/bash

APP_HOME="$(dirname $0)/.."
PATH="${PATH}:${APP_HOME}/../../bin"

export FABRIC_HOME="${APP_HOME}"

for i in customer bankweb airlineweb ; do
  fab --worker $i --make-principal broker
done


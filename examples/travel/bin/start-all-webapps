#!/bin/bash

BIN_DIR="$(dirname $0)"

xterm -title "Customer webapp worker" -e "${BIN_DIR}/start-customer-webapp" &
sleep 1
xterm -title "Bank webapp store" -e "${BIN_DIR}/start-bank-webapp" &
sleep 1
xterm -title "Airline webapp worker" -e "${BIN_DIR}/start-airline-webapp" &


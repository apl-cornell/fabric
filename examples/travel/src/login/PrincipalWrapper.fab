package login;


public class PrincipalWrapper[principal servP, label L] {
    
    public PrincipalWrapper() {}

    principal{L} currentUser = null;
    void currentUser{L}(principal{L} cu) { 
        currentUser = cu; 
    }
    
}

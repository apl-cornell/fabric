package jif.lang;

/**
 * The Jif signature for the method toString()
 *
 * Since the information that is returned by the toString() method
 * will contain information about the fields of a class, this interface
 * is parameterized on a label.
 */
public interface ToStringable[covariant label L] {
    String{L} toString();
}

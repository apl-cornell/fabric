package jif.types.label;

/**
 * The class <code>NotTaken</code> is the label for paths which cannot be
 * taken, for example, the path that includes statements following a return statement.
 */
public interface NotTaken extends Label {
}

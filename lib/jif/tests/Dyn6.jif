//:package polyglot.ext.jif.tests;

class Dyn6 {
    public static void g{*!:*}(label{*L} L) {
	int{*L} x = 1;
	x = f(L, x);
	return;
    }

    public static int{*M} f(label M, int{*M} y) { return y+1; }
}

class C authority (Alice) {
    void m{*!:}() where authority (Alice) {
	boolean{Alice!:_} bool = false;
	int{Alice:Bob} x = 7;
	if (bool) {
	    // pc here depends on bool
	    int y = declassify(x, {Alice:Bob,Chuck; bool}); // bad, Chuck can influence bool
	}
    }
}

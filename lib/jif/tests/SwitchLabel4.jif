class SwitchLabel4 {
	public int m(label L, int{*L} x):{L} throws (Exception) {
//  	    switch label (x) {
//  		case (int{} xx) return xx; // xx carries info about L!
//  		    else throw new Exception();
//  	    }
	    int retVal = 0;
	    if (L <= new label {}) {
		retVal = x;
	    }
	    else throw new Exception();
	    return retVal;
	}
}

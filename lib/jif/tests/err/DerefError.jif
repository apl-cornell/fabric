public class DerefError {
    public static double{*L} f(final label{} L, final label{} M, double{*M} x) {
    return x;
    }

    public static void g() {
    final label L = new label { Alice : };
    final label M = new label { Bob : };

    double{*M} x = 1.0;
    double{*L} y;

    y = f(L, M, x);
    }
}

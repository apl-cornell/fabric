package jif.lang;

import fabric.client.Core;

public interface ConfPolicy extends Policy {
  public static class $Proxy extends Policy.$Proxy implements ConfPolicy {
    public $Proxy(Core core, long onum) {
      super(core, onum);
    }
  }
}

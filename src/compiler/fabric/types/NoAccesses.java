package fabric.types;

import jif.types.label.Label;

/**
 * The class <code>NoAccesses</code> is the label used for the default begin
 * and end conflict label of methods, indicating that there are no reads or
 * writes performed in the method.
 */
public interface NoAccesses extends Label {
}

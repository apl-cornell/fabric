package sif.servlet;

import java.io.Serializable;

/** A Name is a unique name that can be embedded into a web
 * page as ASCII characters. It is used instead of
 * String because strings have weird behavior because
 * they are made of chars.
 */
public class Name extends java.lang.Object implements Serializable {
    public native int{this} hashCode(); 

    public native boolean{this;o} equals(Object o);
    
    public native String{this} toHex();
}

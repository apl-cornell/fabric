package sif.servlet;

/**
 * A session principal. This principal represents an HTTP session.
 */
public final class SessionPrincipal extends AbstractPrincipal {
    
    public native SessionPrincipal(String{this} sessionId);
    
    public native SessionPrincipal(String{this} sessionId, Principal{this} superior);
    
    public native static SessionPrincipal{P←} declassifySP{P←}(SessionPrincipal{P→;P←} sp_, 
            principal{P←} P)
    where caller(P);

}

package fabric.io;

public class FabFile extends FileSystemObject {
    
    // file attributes should be here
    
    ResizableFileByteArray contents;
    
    public FabFile(String name, FabDirectory parent, String fullPath) {
        super(name, parent, fullPath);
        contents = new ResizableFileByteArray();
    }
}
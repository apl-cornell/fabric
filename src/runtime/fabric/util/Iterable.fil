package fabric.util;

import fabric.client.Core;

public interface Iterable {
  /**
   * Creates an iterator on the given core.
   */
  Iterator iterator(Core core);

  /**
   * Creates an iterator on the local core.
   */
  Iterator iterator();
}


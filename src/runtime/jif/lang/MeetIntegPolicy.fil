package jif.lang;

import fabric.util.*;

/**
 * Represents the meet of integrity policies. This code is mostly copied from
 * Jif.
 */
public final class MeetIntegPolicy extends MeetPolicy implements IntegPolicy {
  MeetIntegPolicy(Set policies) {
    super(policies);
  }

  public IntegPolicy join(Core core, IntegPolicy p, Set s) {
    return LabelUtil.join(core, this, p, s);
  }

  public IntegPolicy meet(Core core, IntegPolicy p, Set s) {
    return LabelUtil.meet(core, this, p, s);
  }

  public IntegPolicy join(Core core, IntegPolicy p) {
    return LabelUtil.join(core, this, p);
  }

  public IntegPolicy meet(Core core, IntegPolicy p) {
    return LabelUtil.meetPol(core, this, p);
  }
}

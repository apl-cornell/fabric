package fabric.lang.arrays.internal;

public class _byteArray {
  public native byte set(int i, byte value);

  public native byte get(int i);

  public _byteArray(Label updateLabel, ConfPolicy accessPolicy, int length) {
  }

  public _byteArray(Label updateLabel, ConfPolicy accessPolicy, byte native[] value) {
  }
}

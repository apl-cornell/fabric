package fabric.lang.arrays.internal;

public class _floatArray {
  public native float set(int i, float value);

  public native float get(int i);

  public _floatArray(Label updateLabel, ConfPolicy accessPolicy, int length) {
  }

  public _floatArray(Label updateLabel, ConfPolicy accessPolicy, float native[] value) {
  }
}

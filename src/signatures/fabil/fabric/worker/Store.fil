package fabric.worker;

public interface Store extends fabric.lang.JavaInlineable {
  NodePrincipal getPrincipal();
  fabric.util.Map getRoot();
  String name();
  // TODO
  //fabric.metrics.DerivedMetric findDerivedMetric(fabric.metrics.DerivedMetric m);
}

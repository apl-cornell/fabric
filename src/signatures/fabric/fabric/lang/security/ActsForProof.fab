package fabric.lang.security;

/**
 * A proof that the principal "actor" may act for the principal "granter"
 */
public abstract class ActsForProof {
    private static int __JIF_SIG_OF_JAVA_CLASS$20030619 = 0;
    
    ActsForProof(principal{this} actor, principal{this} granter) {
    }

    public native principal{this} getActor();

    public native principal{this} getGranter();
}

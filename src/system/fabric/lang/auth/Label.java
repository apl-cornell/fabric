package fabric.lang.auth;

/**
 * A Fabric security label.
 */
public interface Label extends fabric.lang.Object {
  
  fabric.lang.auth.Label DEFAULT = null;

  public static class $Proxy extends fabric.lang.Object.$Proxy implements
      fabric.lang.auth.Label {
    public $Proxy(fabric.client.Core core, long onum) {
      super(core, onum);
    }
  }

}

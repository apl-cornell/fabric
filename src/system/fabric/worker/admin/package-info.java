/**
 * Classes for communicating with a worker instance via its admin port. This
 * allows us to send commands to a worker via the command line, making the
 * worker more scriptable.
 */
package fabric.worker.admin;

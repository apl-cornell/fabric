package merchant;

import fabric.client.Core;
import bank.*;
import carrier.CarrierClient;

public class Merchant[principal mp] authority(mp) {
//	private Core{L} dataLocation;
//    private CarrierClient[L]{L} carr;
//    private BankClient[L]{L} bank;

	Merchant{mp:;mp!:} () where authority(mp) {
//        dataLocation = nodeLocation;
//        carr = new CarrierClient[L](nodeLocation);
        BankClient bankClient = BankMain.requestClient(mp);
	}

	public boolean confirmTransaction (int uid) {
//		atomic {
//            try {
//    			int cp = carr.getTicketPrice(uid);
//                int bb = bank.getBalance(uid);
//                return (cp <= bb);
//            }
//            catch(Exception e) {
//            }
//		}
        return false;
	}
}

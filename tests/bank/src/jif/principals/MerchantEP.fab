package jif.principals;

public class MerchantEP extends ExternalPrincipal {

	public MerchantEP(){
		super("MerchantEP");
	}

	private int{client$!:} dummy;
    private static MerchantEP{client$!:} P;

	//XXX Hack to circumvent a bug where the compiler generated throws an error about needing an argument
    public static fabric.lang.Principal{client$!:} getInstance{client$!:}(fabric.client.Core{client$!:} core) {
	if (P == null && core actsfor client$) {
	    P = new MerchantEP@core();
	}
	return P;
    }

    public static fabric.lang.Principal{client$!:} getInstance{client$!:}() {
	if (P == null) {
	    P = new MerchantEP();
	}
	return P;
    }

}

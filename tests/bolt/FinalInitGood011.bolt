class FinalInit011 {
  int a;
  final int b = 0;

  class Inner {
    int a;
    int b;

    Inner() {
      // Constructor prologues may use "this" to refer to outer objects.
      int a = FinalInit011.this.a;
      int b = FinalInit011.this.b;

      super();
    }
  }
}

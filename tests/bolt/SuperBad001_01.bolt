class C {
  static class Super {
    Super(int x) {}
  }

  static class Derived extends Super {
    Derived() {
      // Super calls may not refer to non-static methods.
      super(m());
    }

    int m() {
      return 0;
    }
  }
}

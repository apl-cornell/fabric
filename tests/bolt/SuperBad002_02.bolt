class C {
  static class Super {
    Super(Object o) {}

    class Inner {}
  }

  static class Derived extends Super {
    Derived() {
      // Super calls may not instantiate inner classes.
      super(new Inner());
    }
  }
}

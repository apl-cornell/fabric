class C {
  static class Super {
    Super(Object o) {}
  }

  static class Derived extends Super {
    Derived() {
      // Super calls may not instantiate anonymous classes.
      super(new Object() {});
    }
  }
}

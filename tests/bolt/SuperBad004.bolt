class SuperBad004 {
  static class Super {
    Super(Object o) {}

    final int x = 0;
  }

  static class Derived extends Super {
    Derived() {
      // Super calls may not refer to superclass components.
      super(x);
    }
  }
}

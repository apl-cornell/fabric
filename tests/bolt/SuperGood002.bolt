class C {
  static class Super {
    Super(int x) {}
  }

  static class Derived extends Super {
    int x = 0;

    Derived() {
      // Super calls may use "this" to refer to fields declared in the current
      // class.
      super(this.x);
    }
  }
}

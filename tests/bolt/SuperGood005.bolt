class SuperGood005 {
  int m() {
    return 0;
  }

  static class Super {
    Super(int x) {}
  }

  class Derived extends Super {
    Derived() {
      // Super calls may refer to an enclosing object.
      super(m());
    }
  }
}

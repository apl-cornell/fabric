

class RemoteCall[principal alice, principal bob] {
    public int{alice→bob} m1{alice←} (int{alice→bob} x) {
        return x+1;
    }
    void m2{alice←} () { 
        FabricClient local = FabricClient.getClient();
        if (local == null) return;
        final RemoteClient w = local.getClient("bob");
        if (w actsfor bob && client$ actsfor alice) {
            int{alice→} y = m1@w(1);
        }
    }    
}

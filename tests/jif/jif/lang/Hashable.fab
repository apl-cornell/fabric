package jif.lang;

/**
 * The Jif signature for the method hashCode().
 *
 * Since the information that is returned by the hashCode() method
 * will contain information about the fields of a class, this interface
 * is parameterized on a label.
 */
public interface Hashable[covariant label L] {
    int{L} hashCode();
}

public class Error01[principal p] {
    int{} y;
    public void foo{}(Error01[p]{p→} x) {
        if (x==null) throw new Error();
        //bar();
        int z = x.y;
    }

    void bar{}() {
        y = 10;
    }
}
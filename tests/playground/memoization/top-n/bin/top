#!/bin/bash

source defs
APP_HOME="$(dirname $0)/.."
PATH="${PATH}:${FABRIC}/bin"

export FABRIC_HOME="${APP_HOME}"

NAME=$1
shift

fab --jvm-cp "${APP_HOME}/classes" \
	--app-home "${APP_HOME}" \
	--name "${NAME}" \
	Worker "$@"

import java.util.Random;

public class Config {
  public static final Store STORE = Worker.getWorker().getStore("store");

  public final Random RAND;
  public final double WRITE_RATIO;
  public final int NUM_USERS;
  public final int TOP_COUNT;
  public final double POWER;

  public Config(double writeRatio, int numUsers, int topCount, int seed, double power) {
    this.RAND = new Random(seed);
    this.WRITE_RATIO = writeRatio;
    this.NUM_USERS = numUsers;
    this.TOP_COUNT = topCount;
    this.POWER = power;
  }

  public Config Config$() {
    fabric$lang$Object$();
    return this;
  }
}

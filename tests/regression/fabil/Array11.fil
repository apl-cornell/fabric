public class Array11 extends java.lang.Object {
    // adapted from fabric ArrayTest02

    void m() {
        int    native[] y = new int native[10];
        String native[] z = new String native[10];
        for (int i = 0; i < 10; i++) {
        	y[i] = i;
        	z[i] = "int:" + i;
        }
    }
}

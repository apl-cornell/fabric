public class Array01 {
    void m{*!:*}() throws (Exception{*!:*}) {
        FabricWorker cl = FabricWorker.getWorker();
        if (cl == null) return;
        Store localStore = cl.getLocalStore();
        int{Bob:}[] y = new int[10];
        String{Alice:}[] z = new String[10]@localStore;
        for (int i = 0; i < 10; i++) {
        	y[i] = i;
        	z[i] = "int:" + i;
        }
    }
}

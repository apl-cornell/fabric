public class DynamicL01[principal p] {
    int{} y;
    public void foo{}(label{p→} l) {
        final label m = new label {{*l} ⊓ {⊤←}};
        boolean{} b = false;
        bar(b);
    }

    void bar{}(boolean{} b) {
        y = 10;
    }
}

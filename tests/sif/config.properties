# SIF configuration properties.

# Base directory of Jif installation
#   Will override the value defined in environment variable $JIF.

#jif.home=/usr/local/jif

# Tomcat 5 installation directory
#   Will override the value defined in environment variable $CATALINA_HOME.
#catalina.home=/usr/local/tomcat

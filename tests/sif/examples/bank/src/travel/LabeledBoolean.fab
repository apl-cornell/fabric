package travel;

public class LabeledBoolean {
    
    public LabeledBoolean(label{*lbl} lbl, boolean{*lbl} value) {
        this.lbl = lbl;
        this.value = value;
    }
    public final label{*lbl} lbl;
    public boolean{*lbl} value;
}
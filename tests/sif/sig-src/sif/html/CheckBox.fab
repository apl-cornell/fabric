package sif.html;

import sif.servlet.*;

public final class CheckBox[principal servletP, label L, label E] extends InputNode[servletP, L, E] {
    private static int __JIF_SIG_OF_JAVA_CLASS$20030619 = 0;
    private static int __JIF_PARAMS_RUNTIME_REPRESENTED$20051007 = 0;

    public CheckBox(Input[servletP]{L} input, boolean{L} checked_) 
    where {L;E} <= {*input.inputLbl} { 
        super(input);
    }
//    public CheckBox{L}(Servlet[servletP]{L} s, label{L} inputLabel, boolean{L} checked_) 
//    where {L} <= {*inputLabel} { 
//        super(new Input[servletP](s, inputLabel));
//    }    
}

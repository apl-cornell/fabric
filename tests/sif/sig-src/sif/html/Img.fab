package sif.html;

import sif.servlet.*;

/**
 * A image -- an "img src=" tag.
 */
public final class Img[label L, label E] extends Tag[L, E] extends java.lang.Object {
    private static int __JIF_SIG_OF_JAVA_CLASS$20030619 = 0;
    private static int __JIF_PARAMS_RUNTIME_REPRESENTED$20051007 = 0;

    private final String{L} url;
    public Img{L}(String{L} url_) {
        super("img", null);        
        this.url = url_;
    }
    public Img{L}(String{L} url_, String{L} c) {
        super("img", c);
        this.url = url_;
    }
}

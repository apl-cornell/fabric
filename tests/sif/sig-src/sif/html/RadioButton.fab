package sif.html;


/**
 * Signature for the RadioButton class
 *
 */
public final class RadioButton[principal servletP, label L, label E] extends InputNode[servletP, L, E] {
    private static int __JIF_SIG_OF_JAVA_CLASS$20030619 = 0;
    private static int __JIF_PARAMS_RUNTIME_REPRESENTED$20051007 = 0;

    public RadioButton(Input[servletP]{L} i, String{L} value, boolean{L} checked_) 
    where {L;E} <= {*i.inputLbl} {
	super(i);
    }

}

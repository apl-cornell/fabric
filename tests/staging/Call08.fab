abstract class Call08[label L1, label L2] where L2 ⊑ {W2R(L1)} {
  void m@{W2R(L1)}()@{L2} {
    f();
    // Shouldn't be able to call f() a second time.
    f();
  }

  abstract void f@{W2R(L1)}()@{L2};
}

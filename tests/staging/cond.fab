public class cond {

  public boolean b() {
    return true;
  }

  public void wat1@{*->}(final label l1, final label l2)@{_->} where l2 <= l1 {
    int y = b() ? wat2(l2) : wat2(l1);
    final label empty = new label{_->};
    wat2(empty);
  }

  public int wat2@{*l}(final label l)@{*l} {
    return 0;
  }

}

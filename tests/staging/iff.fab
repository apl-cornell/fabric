public class iff {

  public boolean b@{*l}(final label l)@{*l} {
    return true;
  }

  public void wat1@{*->}(final label l1, final label l2, final label l3)@{_->} where l3 <= l1, l2 <= l1 {
    if (b(l1)) {
      wat2(l2);
    } else {
      wat2(l3);
    }
    final label empty = new label{_->};
    wat2(empty);
  }

  public int wat2@{*l}(final label l)@{*l} {
    return 0;
  }
}

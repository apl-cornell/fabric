Fabric GUI Browser
==================

A GUI for browsing a store's object database.

Using
-----
Compile by running `ant` in the storebrowser directory.

Run using the `store-browser` script in the top-level `bin` directory.
For usage information, use the `--help` switch.

